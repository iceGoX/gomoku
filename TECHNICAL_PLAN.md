# 五子棋在线版技术规划

## 产品目标

核心任务不是展示棋盘，而是在电脑、平板和手机上稳定完成一局可信的五子棋：落子不误触、在线双方看到同一状态、专业规则不会被客户端绕过、离线时不依赖网络。

## 当前实现

### 浏览器

- 原生 HTML、CSS、JavaScript，无编译和打包步骤。
- 15×15 交叉点棋盘使用语义化按钮，支持触控、鼠标、键盘焦点与屏幕阅读器标签。
- 落子状态机为 `空闲 → 虚子预览 → 同点二次确认 → 提交`。点击其他点只移动虚子，非法点显示红色虚子并解释原因。
- 离线棋局完全保存在当前页面内，不连接房间 API。
- 在线客户端只做即时预览；服务端响应与 SSE 状态是最终事实来源。

### 服务端

- Python 标准库 `ThreadingHTTPServer`，房间状态保存在进程内存。
- 私人房间固定两人：房主执黑、加入者执白，房主在双方到齐后开局。
- 玩家用随机 ID 与随机令牌鉴权；令牌不进入 URL 和公开房间状态。
- 每次落子包含 `expectedVersion`，版本不一致时拒绝提交并返回最新房间。
- SSE 通过带鉴权请求头的流式 `fetch` 建立，不把身份令牌放入查询参数。
- 等待房间、进行中房间与已结束房间按不同期限清理。

## 规则引擎

### 普通模式

- 15×15 棋盘，黑方先行，双方交替落在空交叉点。
- 横、竖、两条斜线任一方向连续五子或更多即胜。
- 不判断三三、四四或长连禁手。

### 专业模式

- 黑方首手必须落在天元。
- 黑方恰好五连优先判胜。
- 黑方未形成恰好五连时，长连、两个及以上四、两个及以上真实活三均为禁手。
- 四的识别按“再下一子可形成恰好五连”的有效威胁去重，活四的两个胜点不会被误数成两个四。
- 活三必须能通过一个合法黑方着法形成两端都可获胜的直四；延伸点若造成长连、四四或递归三三，该三不计入。
- 白方没有禁手，五连与长连均获胜。

当前递归禁手分析设有深度保护，覆盖实际对局中的递归假活三。若用于正式裁判认证，下一阶段应导入 RIF 禁手题库和跨语言测试向量，以数据证明与指定裁判规则版本完全一致。

## 在线协议

- `POST /api/rooms`：创建房间。
- `POST /api/rooms/{code}/join`：加入房间。
- `POST /api/rooms/{code}/start`：房主开始。
- `POST /api/rooms/{code}/place`：服务端权威落子。
- `POST /api/rooms/{code}/resign`：认输并结束。
- `POST /api/rooms/{code}/rematch`：投票再来一局。
- `POST /api/rooms/{code}/leave`：离开房间。
- `GET /api/rooms/{code}/events`：SSE 房间状态流。

## 响应式与触控

- 桌面：棋盘与信息面板左右排列，棋盘受视口高度约束，避免首屏纵向滚动过长。
- iPad 与窄屏：信息面板移动到棋盘下方，关键确认按钮保留完整触控面积。
- 手机：棋盘占满可用宽度，坐标与棋子按比例缩放，所有主要按钮高度不低于 42px。
- 触控落子统一使用双击确认，不依赖悬停、拖拽或长按。

## 后续演进

1. 用 RIF 官方禁手题集建立 Python 与 JavaScript 的共享测试向量，消除两个实现长期漂移。
2. 按目标赛事增加可选择的 Soosyrv/Taraguchi 等正式开局状态机，而不是把开局规则混入禁手判断。
3. 真实使用出现跨重启恢复需求后，将房间事件写入 SQLite；出现多实例需求后再引入共享状态和消息广播。
4. 增加观战、棋谱导出、计时器、悔棋协商和 AI，但不让这些功能进入首局核心路径。
5. 建立桌面、iPad 横竖屏、主流手机视口的自动化视觉回归。

## 验收标准

- 在线双方创建、加入、开局、交替落子、胜负、认输、再来一局均由服务端裁决并实时同步。
- 第三名玩家不能加入满员房间，非当前玩家不能落子，过期版本不能覆盖新状态。
- 普通模式允许长连；专业模式正确处理天元首手、长连、四四、三三、黑方恰好五连和白方长连。
- 第一次点击永不落下实子，第二次点击同点或点击明确确认按钮才提交。
- 320px 宽手机、iPad 竖屏、iPad 横屏和桌面均无横向溢出。
- 公开仓库不包含真实域名、服务器地址和凭据。
