# AGENTS.md

本文件适用于弈五（Gomoku）项目。遵守上级目录的项目路由和开发要求；在本仓库中执行读取、编辑、测试与 Git 操作。开始前阅读 `README.md`、检查 `git status --short --branch`，保留已有改动。文本文件末尾不增加空行。

## 已确认的页面设计方向

用户于 2026-09-07 确认「奶油潮玩」修订稿中的「弈五 · 原木新中式」。对应设计板为大厅与候场、三端对局、弹窗与状态。后续页面设计与 UI 实现默认沿用这一方向，不需要重新探索整套风格；用户的新要求优先。

这是设计目标，不代表当前代码已经实现。只修改当前任务涉及的页面，不因本文件存在而自动启动全站重设计、提交、推送或部署。

## 可复用的页面设计提示词

```text
为网页版五子棋「弈五」设计或实现页面，沿用已确认的「奶油潮玩 + 原木新中式」方向。
整体年轻、温暖、轻盈、安静，具有现代中国风。使用宣纸白底色、青瓷绿主操作、墨色文字，少量朱红用于印章、错误和危险操作。
棋盘是浅色天然原木：细腻真实木纹、低反光、浅枫木色、柔和木质边缘；网线清晰，黑白陶瓷棋子易于分辨。
采用现代宋体标题和清晰无衬线正文，最多两套字体。竹叶与小印章只在大厅、候场或结果的留白处少量出现，不遮挡棋盘和操作。
保留奶油风格的柔和圆角、轻微阴影和足够留白。不要做成厚重仿古、红金宫廷、深色电竞或装饰堆叠的界面。
聚焦真实功能：创建与加入房间、普通与专业规则、候场、在线与离线对局、规则、结算、悔棋、换先后手、认输和退出确认。
桌面棋盘在左、操作在右；平板和手机优先棋盘在上、操作在下。棋盘始终为正方形，15×15 个交叉点，棋子落在交叉点上。
每个页面突出一个主操作，保留真实状态、错误提示与禁用反馈。不新增账户、商城、货币、排行榜、聊天或计时功能。
生成样图时注明目标视口，使用正视网页内容，不添加手机外壳、系统状态栏或透视变形；实现时以真实规则、可读性与交互正确性校正样图误差。
```

## 视觉与内容要求

- 建议色板：宣纸白 `#FCF8EF`、浅枫木 `#DAB884`、青瓷绿 `#79A99A`、墨色 `#293B34`、朱红 `#B5634F`。这是起始色板，允许为可读性调整明暗；浅绿按钮使用深色文字或加深底色。
- 标题可用现代宋体；正文、表单、按钮使用清晰无衬线。正文以 14–16 CSS px 为基准，主要点击目标至少 44×44 CSS px。
- 优先通过留白、对齐、字体层级和分隔线组织信息，再使用浅色分区。圆角约 16–18px，避免每项都套卡片、卡片嵌套和浓重阴影。
- 原木纹理仅服务棋盘质感，不能降低网线、星位、虚子或黑白棋子的辨识度。不要把木纹画成纯黄色填充，也不要让装饰叠在落点上。
- 当前回合、选中、禁手、等待和失败均以文字或图标辅助颜色表达；不显示虚构进度点、分数或玩家数据。
- 页面文案简洁自然，不把服务端校验、SSE 等实现细节当作用户需要阅读的产品说明。

## 页面、浮层与状态覆盖

本项目是单页应用；模式、角色和状态复用页面，不为每种组合增加独立路由。设计相关页面时覆盖下表中适用的状态。

| 页面或浮层 | 必须保留的内容与状态 |
| --- | --- |
| 创建大厅 | 昵称、普通/专业规则选择、创建双人房间、离线双人入口；提交中与失败 |
| 加入大厅 | 昵称、六位字母数字房间号、加入；无效代码、房间不存在、身份失效等反馈 |
| 候场 | 房间号与复制、黑白座位、空座/齐人、房主开始/访客等待、交换先后手、退出 |
| 对局 | 在线/离线、普通/专业、黑白玩家、自己/对方回合、回合数、最近落子 |
| 落点操作 | 未选中、虚子预览、合法待确认、禁手提示、提交中、确认按钮启停 |
| 规则 | 普通胜负、专业禁手、首手天元、轻点预览与再次确认、连珠核心规则边界 |
| 结果 | 黑胜、白胜、和棋、认输结果；再来一局、交换先后手、回到大厅 |
| 再来一局 | 离线重开；在线邀请、等待对方、双方同意后重开 |
| 换手请求 | 发起方等待、接收方同意/拒绝；候场换手与结束后换手重开的不同说明 |
| 悔棋请求 | 剩余次数、申请等待、同意/拒绝、协商期间回合锁定 |
| 认输确认 | 继续对局、确认认输；说明认输后对方获胜 |
| 退出确认 | 在线认输并返回、离线结束且不保存、候场退出房间分别说明 |
| 通用反馈 | 复制成功/失败、重连/恢复、房间或身份失效、请求失败及可执行的下一步 |

## 交互与规则边界

- 加入房间时规则由房间决定，不能让加入页的普通/专业控件修改房间规则。离线入口沿用所选规则，无需新增离线设置页。
- 房间号是六位字母数字代码，不设计为只能输入数字的验证码。离线界面不显示房间号、复制邀请或在线连接状态。
- 保留第一次轻点显示虚子、再次点击同一交叉点或点击「确认落子」提交的防误触路径。更换落点只移动预览。
- 普通模式五子及以上连线获胜。专业模式黑方首手天元，禁止三三、四四、长连，黑方恰好五连获胜，白方五连或更长获胜。和棋必须满足棋盘已满且没有有效胜局。
- 保持服务端权威，不为贴合图片绕过回合、禁手、胜负、悔棋、换手或再来一局的校验。修改同步链路时检查玩家令牌、`expectedVersion`、SSE 重连、过期响应与状态一致性。
- 悔棋、认输、退出等按钮必须依据真实局面启停；不能因视觉统一移除协商、确认或次数限制。

## 响应式与可访问性

- 桌面参考 1440×1024：大厅左标题与小插画、右表单；对局棋盘在左、操作与最近落子在右。
- 平板竖屏参考 834×1194：棋盘在上，操作在下；横屏空间足够时采用左右布局。
- 手机参考 390×844：单列、缩小装饰，棋盘尽量用满可用宽度，确认区保持可达；最近落子可折叠。输入法弹出后仍能滚动到表单提交按钮。
- 视口尺寸用于设计参考，不是固定宽高。所有视口保持正方形棋盘和等距交叉点，不拉伸棋盘填满屏幕，不以缩小点击目标解决拥挤。
- 规则面板可滚动；短确认使用紧凑浮层。键盘焦点可见，模态框正确管理焦点并在关闭后恢复，按钮具备明确名称。

## 样图误差与实现检查

- 图片只约束风格与信息层级。错误坐标、网格数量、棋盘比例、棋子位置、小字与错别字不能直接照搬；规则示意必须真的画出连续五子，不能复制样图中的四子示意。
- 使用真实状态渲染文案与按钮，不将整张样图作为交互界面，不把示例房间号或玩家名称硬编码为运行数据。
- 先通过 `rg` 定位 `index.html`、`styles.css`、`game.js` 中的入口和状态绑定，再作最小修改；纯视觉调整不更换框架或游戏引擎。
- 实际 UI 改动检查桌面、平板、手机及鼠标/触控路径，分别报告静态检查、自动测试与浏览器验证结果。
- 规则或交互变化按范围运行 `python3 -m unittest discover -s tests -v`、`node --test tests/test_undo_ui.mjs`。仅修改本文件时做文档和 diff 检查，不必运行游戏测试。
- 收尾检查 `git status --short`、`git diff --check`。提交、推送与部署仅在用户明确要求时执行；生产子路径为 `/wuziqi/`，发布细节以当前 `README.md` 和 `deployment/` 为准。

## 可编辑设计稿与落地记录

- [Pixso 设计文件](https://pixso.net/app/design/ns1Iu4bwK_F6eC46uOjtAA)，位于 `icego.tech` 文件夹。
- [设计与视觉资源说明](docs/design/README.md)、[本地 UI 验证记录](design-qa.md)。
